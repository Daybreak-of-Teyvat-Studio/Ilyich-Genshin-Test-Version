SNE_surrender = {

#投降
	SNE_surrender_to_MOT = {
		icon = GFX_decision_generic_ignite_civil_war
		available = {
			surrender_progress > 0.1
		}
		visible = {
			has_war_with = MOT
			has_war_with = DVA
		}
		days_remove = 7
		fire_only_once = yes
		ai_will_do = {
			base = 100
		}
		cost = 0
		complete_effect = {   
			DVA = {
				country_event = dva.7
			}
		}
		remove_effect = {   
			every_country = {
				limit = {
					has_war_with = SNE
				}
				white_peace = SNE
			}
			DVA = {
				annex_country = {
					target = SFS
					transfer_troops = no
				}
			}
		}	
	}

}

SNE_vs_PRI = {
	SNE_vs_PRI_1 = {
		# 

		allowed = {
			tag = SNE
		}

		available = {
			PRI = {
				OR = {
					exists = no
					is_in_faction_with = SNE
				}
			}
		}
		days_mission_timeout = 180
		fire_only_once = yes
		is_good = no
		activation = {
			has_war_with = PRI
		}


		complete_effect = {
			add_political_power = 100
			add_stability = 0.10
		}

		timeout_effect = {
			add_stability = -1
			add_political_power = -100
            SNE = {
				country_event = { 
					id = SNE_fw.2
					days = 1 
				}  
			}
		}

		ai_will_do = {
			factor = 1
		}
	}
		



}
