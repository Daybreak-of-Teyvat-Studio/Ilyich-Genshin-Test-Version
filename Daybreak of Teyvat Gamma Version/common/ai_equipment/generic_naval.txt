destroyers = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_screen }

	priority = {
		factor = 2000
	}

	screen_1 = {
		priority = {
			factor = 2000
			modifier = {
				has_tech = basic_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 10000.0
			type = ship_hull_light_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = empty

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_torpedo } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_light_battery
			ship_torpedo
			light_ship_engine
		}
	}

	screen_2 = {
		priority = {
			factor = 3000
			modifier = {
				has_tech = improved_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 20000.0
			type = ship_hull_light_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = empty

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_torpedo } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_light_battery
			ship_torpedo
			light_ship_engine
		}
	}

	screen_3 = {
		priority = {
			factor = 4000
			modifier = {
				has_tech = advanced_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 30000.0
			type = ship_hull_light_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_torpedo } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_light_battery
			ship_torpedo
			light_ship_engine
		}
	}

	screen_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 2

		target_variant = {
			match_value = 40000.0
			type = ship_hull_light_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { module = dp_light_battery_4 upgrade = current }
				fixed_ship_torpedo_slot = empty

				# CUSTOM
				front_1_custom_slot = { module = dp_light_battery_4 upgrade = current }
				mid_2_custom_slot = { module = dp_light_battery_4 upgrade = current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { module = dp_light_battery_4 upgrade = current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			dp_light_battery
			ship_light_battery
			light_ship_engine
		}
	}
}

generic_escorts = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_escort }

	priority = {
		factor = 2000

	}

	escort_1 = {
		priority = {
			factor = 2000
			modifier = {
				factor = 4000
				anti_submarine_strategy_required_trigger = yes
			}
			modifier = {
				has_tech = basic_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 10000.0
			type = ship_hull_light_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_sonar } upgrade > current }
				fixed_ship_radar_slot = empty

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = empty

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			ship_sonar
			ship_light_battery
			light_ship_engine
		}
	}

	escort_2 = {
		priority = {
			factor = 3000
			modifier = {
				factor = 4000
				anti_submarine_strategy_required_trigger = yes
			}
			modifier = {
				has_tech = improved_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 20000.0
			type = ship_hull_light_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_sonar } upgrade > current }
				fixed_ship_radar_slot = empty

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = empty

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			ship_sonar
			ship_light_battery
			light_ship_engine
		}
	}

	escort_3 = {
		priority = {
			factor = 4000
			modifier = {
				factor = 4000
				anti_submarine_strategy_required_trigger = yes
			}
			modifier = {
				has_tech = advanced_ship_hull_light
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 30000.0
			type = ship_hull_light_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_sonar } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { any_of = { ship_light_battery } upgrade > current }
				fixed_ship_torpedo_slot = empty

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			ship_sonar
			ship_radar
			ship_light_battery
			light_ship_engine
		}
	}

	escort_4 = {
		priority = {
			factor = 5000
			modifier = {
				factor = 4000
				anti_submarine_strategy_required_trigger = yes
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 40000.0
			type = ship_hull_light_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_sonar } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = light_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# MAIN
				fixed_ship_battery_slot = { module = dp_light_battery_4 upgrade = current }
				fixed_ship_torpedo_slot = empty

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_depth_charge } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			ship_sonar
			ship_radar
			dp_light_battery
			ship_light_battery
			light_ship_engine
		}
	}
}

naval_light_cruiser = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_cruiser_light }

	priority = {
		factor = 2000

	}

	cruiser_light_1 = {
		priority = {
			factor = 2000
			modifier = {
				factor = 0
				has_tech = basic_ship_hull_cruiser
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 10000.0
			type = ship_hull_cruiser_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_light_medium_battery } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_light_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_light_2 = {
		priority = {
			factor = 3000
			modifier = {
				factor = 0
				has_tech = improved_ship_hull_cruiser
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 20000.0
			type = ship_hull_cruiser_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_light_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_light_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_light_3 = {
		priority = {
			factor = 4000
			modifier = {
				factor = 0
				has_tech = advanced_ship_hull_cruiser
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 30000.0
			type = ship_hull_cruiser_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_light_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				rear_2_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_light_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_light_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 2

		target_variant = {
			match_value = 40000.0
			type = ship_hull_cruiser_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_light_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
				rear_2_custom_slot = { any_of = { ship_light_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_light_medium_battery
			ship_dp_secondaries
			ship_secondaries
			cruiser_ship_engine
		}
	}
}

naval_cruiser_heavy = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_cruiser_heavy }

	priority = {
		factor = 2000

	}

	cruiser_heavy_1 = {
		priority = {
			factor = 2000
			modifier = {
				factor = 0
				has_tech = basic_ship_hull_cruiser
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 10000.0
			type = ship_hull_cruiser_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_medium_battery } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_heavy_2 = {
		priority = {
			factor = 3000
			modifier = {
				factor = 0
				has_tech = improved_ship_hull_cruiser
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 20000.0
			type = ship_hull_cruiser_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_heavy_3 = {
		priority = {
			factor = 4000
			modifier = {
				factor = 0
				has_tech = advanced_ship_hull_cruiser
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 30000.0
			type = ship_hull_cruiser_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				rear_2_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_medium_battery
			ship_secondaries
			cruiser_ship_engine
		}
	}

	cruiser_heavy_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 1

		target_variant = {
			match_value = 40000.0
			type = ship_hull_cruiser_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = cruiser_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = empty

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_medium_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
				rear_2_custom_slot = { any_of = { ship_medium_battery } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_medium_battery
			ship_dp_secondaries
			ship_secondaries
			cruiser_ship_engine
		}
	}
}

naval_capital_battleship = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_capital_bb }

	priority = {
		factor = 2000

	}

	battleship_1 = {
		priority = {
			factor = 2000
			modifier = {
				has_tech = basic_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 10000.0
			type = ship_hull_heavy_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bb_1 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bb_1
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_secondaries
			heavy_ship_engine
		}
	}

	battleship_2 = {
		priority = {
			factor = 3000
			modifier = {
				has_tech = improved_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 20000.0
			type = ship_hull_heavy_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bb_2 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bb_2
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_secondaries
			heavy_ship_engine
		}
	}

	battleship_3 = {
		priority = {
			factor = 4000
			modifier = {
				has_tech = advanced_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 30000.0
			type = ship_hull_heavy_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bb_2 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_3_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bb_2
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_secondaries
			heavy_ship_engine
		}
	}

	battleship_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 1

		target_variant = {
			match_value = 40000.0
			type = ship_hull_heavy_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bb_3 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_3_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bb_3
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_dp_secondaries
			ship_secondaries
			heavy_ship_engine
		}
	}
}

naval_super_heavy_battleship = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_capital_shbb }

	priority = {
		factor = 3500

	}

	super_heavy_battleship_1 = {
		priority = {
			factor = 3500
		}

		role_icon_index = 1

		target_variant = {
			match_value = 25000.0
			type = ship_hull_super_heavy_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_shbb upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_super_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_super_heavy_battery } upgrade > current }
				front_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_3_custom_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_super_heavy_battery } upgrade > current }
				rear_2_custom_slot = { any_of = { ship_super_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_shbb
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_super_heavy_battery
			ship_heavy_armor
			ship_dp_secondaries
			ship_secondaries
			heavy_ship_engine
		}
	}
}

naval_capital_bc = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_capital_bc }

	priority = {
		factor = 1500

	}

	battlecruiser_1 = {
		priority = {
			factor = 1500
			modifier = {
				has_tech = basic_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 7500.0
			type = ship_hull_heavy_1

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bc_1 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bc_1
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_secondaries
			ship_heavy_armor
			heavy_ship_engine
		}
	}

	battlecruiser_2 = {
		priority = {
			factor = 2500
			modifier = {
				has_tech = improved_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 15000.0
			type = ship_hull_heavy_2

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bc_2 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bc_2
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_secondaries
			ship_heavy_armor
			heavy_ship_engine
		}
	}

	battlecruiser_3 = {
		priority = {
			factor = 3500
			modifier = {
				has_tech = advanced_ship_hull_heavy
				factor = 0
			}
		}

		role_icon_index = 1

		target_variant = {
			match_value = 25000.0
			type = ship_hull_heavy_3

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bc_2 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_3_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bc_2
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_secondaries
			heavy_ship_engine
		}
	}

	battlecruiser_4 = {
		priority = {
			factor = 4500
		}

		role_icon_index = 1

		target_variant = {
			match_value = 35000.0
			type = ship_hull_heavy_4

			modules = {
				# ELECTRONICS
				fixed_ship_fire_control_system_slot = { any_of = { ship_fire_control_system } upgrade > current }
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = heavy_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { module = ship_armor_bc_3 upgrade = current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_battery_slot = { any_of = { ship_heavy_battery } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				mid_3_custom_slot = { any_of = { ship_anti_air } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_heavy_battery } upgrade > current }
			}
		}

		requirements = {
			module = ship_armor_bc_3
		}

		allowed_modules = {
			ship_anti_air
			ship_fire_control_system
			ship_radar
			ship_heavy_battery
			ship_heavy_armor
			ship_dp_secondaries
			ship_secondaries
			heavy_ship_engine
		}
	}
}

naval_carrier = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_carrier }

	priority = {
		factor = 2000

	}

	carrier_1 = {
		priority = {
			factor = 2000
			modifier = {
				has_tech = basic_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 7

		target_variant = {
			match_value = 10000.0
			type = ship_hull_carrier_conversion_bb

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_2 = {
		priority = {
			factor = 3000
			modifier = {
				has_tech = improved_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 7

		target_variant = {
			match_value = 20000.0
			type = ship_hull_carrier_1

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_3 = {
		priority = {
			factor = 4000
			modifier = {
				has_tech = advanced_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 7

		target_variant = {
			match_value = 30000.0
			type = ship_hull_carrier_2

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 7

		target_variant = {
			match_value = 40000.0
			type = ship_hull_carrier_3

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_dp_secondaries
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}
}

naval_carrier_light = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_carrier_light }

	priority = {
		factor = 2000

	}

	carrier_light_1 = {
		priority = {
			factor = 2000
			modifier = {
				has_tech = basic_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 10000.0
			type = ship_hull_carrier_conversion_bb

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_1 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_light_2 = {
		priority = {
			factor = 3000
			modifier = {
				has_tech = improved_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 20000.0
			type = ship_hull_carrier_1

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_2 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_light_3 = {
		priority = {
			factor = 4000
			modifier = {
				has_tech = advanced_ship_hull_carrier
				factor = 0
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 30000.0
			type = ship_hull_carrier_2

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_3 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}

	carrier_light_4 = {
		priority = {
			factor = 5000
		}

		role_icon_index = 3

		target_variant = {
			match_value = 40000.0
			type = ship_hull_carrier_3

			modules = {
				# ELECTRONICS
				fixed_ship_radar_slot = { any_of = { ship_radar } upgrade > current }

				# ENGINE
				fixed_ship_engine_slot = { module = carrier_ship_engine_4 upgrade = current }

				# ANTI-AIR
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air } upgrade > current }

				# ARMOR
				fixed_ship_armor_slot = { any_of = { ship_cruiser_armor } upgrade > current }

				# MAIN
				fixed_ship_secondaries_slot = { any_of = { ship_dp_secondaries } upgrade > current }
				fixed_ship_deck_slot_1 = { any_of = { ship_deck_space } upgrade > current }
				fixed_ship_deck_slot_2 = { any_of = { ship_deck_space } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				mid_2_custom_slot = { any_of = { ship_deck_space } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_deck_space } upgrade > current }
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_radar
			ship_deck_space
			ship_dp_secondaries
			ship_secondaries
			ship_fire_control_system
			carrier_ship_engine
			ship_cruiser_armor
		}
	}
}

naval_submarine = {
	category = naval

	blocked_for = { }
	available_for = { }

	roles = { naval_submarine }

	priority = {
		factor = 2000

	}

	submarine_1 = {
		priority = {
			factor = 2000
			modifier = {
				has_tech = basic_ship_hull_submarine
				factor = 0
			}
		}
		role_icon_index = 6

		target_variant = {
			match_value = 10000.0
			type = ship_hull_submarine_1

			modules = {
				# ENGINE
				fixed_ship_engine_slot = { module = sub_ship_engine_1 upgrade = current }

				# MAIN
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo_sub } upgrade > current }

				# CUSTOM
				rear_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
			}
		}

		allowed_modules = {
			ship_torpedo_sub
			sub_ship_engine
		}
	}

	submarine_2 = {
		priority = {
			factor = 3000
			modifier = {
				has_tech = improved_ship_hull_submarine
				factor = 0
			}
		}
		role_icon_index = 6

		target_variant = {
			match_value = 20000.0
			type = ship_hull_submarine_2

			modules = {
				# ENGINE
				fixed_ship_engine_slot = { module = sub_ship_engine_2 upgrade = current }

				# MAIN
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo_sub } upgrade > current }

				# CUSTOM
				rear_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
			}
		}

		allowed_modules = {
			ship_torpedo_sub
			sub_ship_engine
		}
	}

	submarine_3 = {
		priority = {
			factor = 4000
			modifier = {
				has_tech = advanced_ship_hull_submarine
				factor = 0
			}
		}
		role_icon_index = 6

		target_variant = {
			match_value = 30000.0
			type = ship_hull_submarine_3

			modules = {
				# ENGINE
				fixed_ship_engine_slot = { module = sub_ship_engine_3 upgrade = current }

				# MAIN
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo_sub } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_sub_snorkel } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
			}
		}

		allowed_modules = {
			ship_torpedo_sub
			sub_ship_engine
			ship_sub_snorkel
		}
	}

	submarine_4 = {
		priority = {
			factor = 5000
		}
		role_icon_index = 6

		target_variant = {
			match_value = 40000.0
			type = ship_hull_submarine_4

			modules = {
				# ENGINE
				fixed_ship_engine_slot = { module = sub_ship_engine_4 upgrade = current }

				# MAIN
				fixed_ship_torpedo_slot = { any_of = { ship_torpedo_sub } upgrade > current }

				# CUSTOM
				front_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
				mid_1_custom_slot = { any_of = { ship_sub_snorkel } upgrade > current }
				rear_1_custom_slot = { any_of = { ship_torpedo_sub } upgrade > current }
			}
		}

		allowed_modules = {
			ship_torpedo_sub
			sub_ship_engine
			ship_sub_snorkel
		}
	}
}