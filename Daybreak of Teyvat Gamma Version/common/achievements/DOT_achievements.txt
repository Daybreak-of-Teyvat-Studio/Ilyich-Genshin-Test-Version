unique_id = DOT_achievements

MOT_end_PRI = {
    possible = {
        # classic triggers used in all vanilla achievements
        difficulty > 1
        has_start_date < 1936.01.02
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
		    tag = MOT
    }
    
    happened = {
		# has_war_with = PRI
    not = {country_exists = PRI}
    }    
}

LYY_end_PRI = {
    possible = {
        # classic triggers used in all vanilla achievements
        difficulty > 1
        has_start_date < 1936.01.02
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
		    tag = LYY
    }
    
    happened = {
		# has_war_with = PRI
    not = {country_exists = PRI}
    }    
}

INA_end_PRI = {
    possible = {
        # classic triggers used in all vanilla achievements
        difficulty > 1
        has_start_date < 1936.01.02
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
		    tag = INA
    }
    
    happened = {
		# has_war_with = PRI
    not = {country_exists = PRI}
    }    
}

SUM_end_PRI = {
    possible = {
        # classic triggers used in all vanilla achievements
        difficulty > 1
        has_start_date < 1936.01.02
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
		    tag = SUM
    }
    
    happened = {
		# has_war_with = PRI
    not = {country_exists = PRI}
    }    
}

FON_end_PRI = {
    possible = {
        # classic triggers used in all vanilla achievements
        difficulty > 1
        has_start_date < 1936.01.02
        has_any_custom_difficulty_setting = no
        game_rules_allow_achievements = yes
		    tag = FON
    }
    
    happened = {
		# has_war_with = PRI
    not = {country_exists = PRI}
    }    
}



