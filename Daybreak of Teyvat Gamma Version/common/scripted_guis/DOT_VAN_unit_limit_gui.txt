scripted_gui = {
    VAN_political_thoughts_ui = {
        context_type = player_context
        window_name = "VAN_unit_limit_window"
        parent_window_token = deployment_tab

        visible = {
            original_tag = VAN
            has_country_flag = VAN_flag_enable_unit_limit
        }
    }
}
